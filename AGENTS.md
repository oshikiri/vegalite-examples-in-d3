# AGENTS Guidelines

## JavaScript一般の話
- 未使用変数や不要なローカル変数・引数があれば削除する。
- 非同期処理は `async`/`await` を使って記述する。
- 高レベルの処理を前に置き、補助関数の定義はその後に記述する。
- 用語や英単語の表記は節を跨いで統一し、表記ゆれがないか適宜確認する。
- 関数引数は原則として位置引数で受け取り、オブジェクト引数は依存が多く順序が読みにくくなる場合のみに限定する。

## レンダリングフローの整理
- レンダリングのトップレベルの関数として、描画フローの概要のみを記述する `render` という名前の関数を定義する。
- `render` は必ずスクリプトの先頭に配置する。
- `render` は返り値として、生成したDOMやスケールを `layout` `scale` `containers` `labels` `axes` など役割別にまとめたものを返す。
- レイアウト定義やスケール生成や描画処理は補助関数に閉じ込め、外部からアクセスできないようにする。
  - `render` 内では D3 の `.append()` や `.attr()` など直接の描画呼び出しをしない。
  - `appendPlotGroup` などの補助関数に委譲して宣言的な記述を保つようにする。
- チャート固有のカテゴリや配列などの定数はできるだけ使用する箇所の近くに置く。
  - 定数を共有する際は小さな `createXxxConfig` のようなヘルパー関数やスケールオブジェクトに集約し、可読性と参照位置の近さを両立させる。
- 塗りや線のスタイル指定は描画ヘルパー内で `.attr()` や `.style()` を使って付与し、HTML内の `<style>` やCSSに依存させない。

## スケールとヘルパー活用
- スケールは `scale.x`/`scale.y` のように1つのオブジェクトへまとめる。関数へは `scale` ごと渡すことでインターフェースを簡潔に保つ。
- 極座標チャートでは `d3.pie` や `d3.arc` といったレイアウト生成もスケールと並べて構成し、角度は累積値を基に計算しつつ半径には平方根スケールと最小半径を適用してVega-Liteの表現を再現する。
- スケール以外にも、同様の依存を持つレイアウトやマージンなども `createChartConfig` のようなヘルパーでまとめて定義する。`render` ではそのヘルパーから設定を受け取るようにする。
- 軸・ラベルなど関連する DOM 参照はオブジェクト（例: `labels`, `axes`）にまとめ、呼び出し元では最小限のオブジェクトを受け取るようにする。
- 内部ヘルパー関数に必要な値（スケールやマージンなど）は、最小限のものを引数として渡し、描画ロジックの責務が明確になるようにする。
- スケールの domain は原則データから導出する。
  - 上下限が一致する場合は自前で少し広げた値を与えることで、ゼロ幅スケールを避ける。
- カラーパレットや凡例設定などの定数は使用する関数スコープに閉じ込め、不要なグローバル依存を避ける。
- カテゴリが事前に既知な場合はスケール初期化時に domain を固定し、レジェンドや色の対応がデータ有無に左右されないようにする。
- カラースケールの順序を制御したい場合は `d3.stack().keys([...])` に希望する並びの配列を渡し、同じ配列でカラースケールの domain も揃えて凡例・色との対応を保つ。
- カテゴリやシンボルに紐づく ordinal スケールは `createScale` 内で生成し、`scale.color.domain()` などで `scale` 経由で共有できるようにする。

## 凡例と軸の共通化
- 凡例は描画ロジックはメソッドにまとめ、チャート固有の設定（ラベル、表示値など）を引数で受け取れるようにする。
- 軸や凡例など DOM ノード参照は役割が分かる識別子（例: `labels.x`, `axes.y`）で保持し、後段処理でも追跡しやすくする。

## Vega-Lite から D3 への書き換え
- `vl.json` を追加・更新するときは Vega-Lite 公式リポジトリ（例: `https://github.com/vega/vega-lite/blob/main/examples/specs/...`）の該当ファイルから取得する。
- `vl.json` が参照するデータは、元の相対パス（例: `data/...`）をプロジェクト構造に合わせて `../../data/...` へ配置する。D3 実装でも同じファイルを読み込む。
- Vega-Lite から移植する際は、凡例やマークの色が変わらないように Vega-Lite のカラースキーム（例: `category10`）を D3 側でも明示的に指定する。
- `layout.chart.width` / `height` は `vl.json` の `width` / `height` を流用し、追加の余白は軸や凡例に必要な分だけマージンで与える。
- レンダリングの一環として外部のCSSは使用せず、JSのみで色を指定するようにする
